pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.minecraftforge.net/' }
    }
}

rootProject.name = "DataSync"

include 'datasync-homes'
include 'datasync-tp'
include 'datasync-inventory'
include 'datasync-ftbquests'
include 'datasync-ftbteams'